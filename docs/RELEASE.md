# Release Snapshot

Suggested release tag: `v0.1.0-mvp`

## Commands

```bash
git add .
git commit -m "Release v0.1.0-mvp: Dockerized PM MVP with auth, persistent kanban, AI sidebar, and CI"
git tag -a v0.1.0-mvp -m "Project Management MVP release"
```

Optional push:

```bash
git push origin main --tags
```
